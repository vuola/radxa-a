apiVersion: v1
kind: Service
metadata:
  name: weather-web
  namespace: weather
spec:
  type: ClusterIP
  selector:
    app: weather-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
